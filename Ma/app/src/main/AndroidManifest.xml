<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="turism.ru">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rest.RestActivity"></activity>
        <activity android:name=".museum.museum" />
        <activity android:name=".Wheel.WheelActivity" />
        <activity android:name=".VkAndInst.VkAndInstURL" />
        <activity android:name=".QR.QRActivity" />
        <activity android:name=".VK.VKActivity" />
        <activity android:name=".MapGoogleActivity" />
        <activity
            android:name=".Menu.NavigationMenuActivity"
            android:label="@string/title_activity_navigation_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Menu.MenuActivity" />
        <activity android:name=".authorization.LoginnActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>